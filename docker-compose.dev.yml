version: '3.8'

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
      - ./data:/data
    environment:
      - DATABASE_URL=sqlite:////data/bibmap.db
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
